[build-system]
requires = [
    "setuptools>=61",
    "setuptools_scm>=8.0.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
root = ".."
local_scheme = "no-local-version"